# RemoteTerm for MeshCore HA app

A minimal Home Assistant app wrapper around RemoteTerm for MeshCore.

This wrapper builds the official RemoteTerm for MeshCore `3.13.0` release, connects RemoteTerm to a MeshCore radio over TCP, and maps Home Assistant options to the upstream application's `MESHCORE_*` environment variables.
